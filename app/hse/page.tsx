import { IconBadge, PageHero, SectionHeading } from '@/components/Site';
const metrics = [['0.00','LTI rate','shield-check'],['100%','Certified crew','certificate'],['24/7','HSE monitoring','clock-check'],['100%','Incident review','clipboard-check']] as const;
export default function HSE() { return <div className="page-shell"><PageHero eyebrow="HEALTH, SAFETY & ENVIRONMENT" title="Safety is" accent="fundamental." copy="A practical, visible culture that protects people, assets and the environment."/><section className="page-section mx-auto max-w-7xl"><SectionHeading eyebrow="Zero Harm Policy" title="The first measure of" accent="performance." copy="Every operation is planned, reviewed and delivered through disciplined safety controls."/><div className="mt-8 grid grid-cols-2 gap-3 lg:max-w-3xl">{metrics.map(([value, label, icon]) => <article className="rounded-[12px] border border-slate-200 bg-white p-4" key={label}><IconBadge name={icon}/><b className="mt-4 block text-xl text-navy">{value}</b><p className="mt-1 text-xs text-slate-600">{label}</p></article>)}</div><div className="mt-10 grid gap-4 border-t border-slate-200 pt-8 md:grid-cols-2">{[['Certifications & assurance','NOGIC JQS-aligned processes, competency assurance and auditable controls support every mobilisation.','certificate'],['Environmental protocols','We prioritise spill prevention, waste management and responsible resource use in every operational plan.','droplet']].map(([title, copy, icon]) => <article className="content-card" key={title}><IconBadge name={icon as 'certificate' | 'droplet'}/><h2 className="mt-4 text-xl text-navy">{title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{copy}</p></article>)}</div></section></div>; }
