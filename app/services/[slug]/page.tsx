import Link from 'next/link'; import { services } from '@/components/data'; import { CheckList, IconBadge, PageHero, SectionHeading } from '@/components/Site';
export default async function Detail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const service = services.find(x => x[0] === slug) || services[0]; return <div className="page-shell"><PageHero eyebrow="SERVICE CAPABILITY" title={service[1]} copy="Proven support designed around safety, availability and accountable outcomes."/><section className="page-section mx-auto grid max-w-7xl gap-6 lg:grid-cols-2"><SectionHeading eyebrow="How We Deliver" title="Responsive expertise for critical" accent="operations." copy="We integrate people, equipment and process to give your project a reliable operational backbone."/><div className="content-card"><IconBadge name="settings"/><h2 className="mt-4 text-xl text-navy">Core capabilities</h2><CheckList items={[...service[2], '24/7 operational coordination', 'HSE-led project governance']}/><div className="button-pair mt-6"><Link href="/contact" className="focus-ring flex min-h-11 items-center justify-center rounded bg-cyan px-3 text-center text-sm font-bold text-navy">Discuss service</Link><Link href="/services" className="focus-ring flex min-h-11 items-center justify-center rounded border border-navy px-3 text-center text-sm font-bold text-navy">All services</Link></div></div></section></div>; }
