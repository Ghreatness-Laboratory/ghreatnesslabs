import Link from 'next/link'; import { services } from '@/components/data'; import { CheckList, IconBadge, PageHero, Reveal, SectionHeading } from '@/components/Site';
const icons = ['anchor', 'ship', 'settings', 'bolt', 'certificate', 'target'] as const;
export default function Services() { return <div className="page-shell"><PageHero eyebrow="SERVICES" title="Operational capability," accent="delivered end to end." copy="Integrated marine, energy and technical support for critical operations."/><section className="page-section mx-auto max-w-7xl"><SectionHeading eyebrow="Our Services" title="Specialist support across marine and" accent="energy."/><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{services.map(([slug, name, items], i) => <Reveal key={slug} className="content-card interactive-card"><IconBadge name={icons[i]}/><h2 className="mt-4 text-xl text-navy">{name}</h2><CheckList items={items}/><Link className="focus-ring mt-4 inline-flex min-h-10 items-center gap-2 text-sm font-bold text-cyan" href={'/services/' + slug}>Learn more <span>→</span></Link></Reveal>)}</div></section></div>; }
