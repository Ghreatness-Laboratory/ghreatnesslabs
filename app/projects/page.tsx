'use client';
import Link from 'next/link'; import { useState } from 'react'; import { IconBadge, PageHero, SectionHeading } from '@/components/Site';
const projects = [['delta-logistics','Marine','Delta offshore logistics','anchor'],['subsea-inspection','Subsea','Asset integrity inspection','settings'],['port-mobilisation','Ports','Terminal mobilisation','ship'],['solar-power','Energy','Remote solar deployment','sun']] as const;
export default function Projects() { const [filter, setFilter] = useState('All'); const shown = filter === 'All' ? projects : projects.filter(x => x[1] === filter); return <div className="page-shell"><PageHero eyebrow="RECENT ENGAGEMENTS" title="Capability in" accent="action." copy="Selected examples of coordinated TASOL delivery in demanding environments."/><section className="page-section mx-auto max-w-7xl"><SectionHeading eyebrow="Case Studies" title="Operational outcomes in the" accent="field."/><div className="mt-7 flex flex-wrap gap-2">{['All','Marine','Subsea','Ports','Energy'].map(x => <button key={x} onClick={() => setFilter(x)} className={'focus-ring min-h-10 rounded px-4 text-sm font-medium '+(filter === x ? 'bg-navy text-white' : 'border border-slate-200 bg-white text-navy')}>{x}</button>)}</div><div className="mt-5 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{shown.map(([slug, category, title, icon]) => <Link key={slug} href={'/projects/'+slug} className="content-card interactive-card"><IconBadge name={icon}/><p className="mt-4 text-xs font-medium text-cyan">{category}</p><h2 className="mt-1 text-xl text-navy">{title}</h2><p className="mt-4 inline-flex items-center text-sm font-bold text-cyan">View case study →</p></Link>)}</div></section></div>; }
