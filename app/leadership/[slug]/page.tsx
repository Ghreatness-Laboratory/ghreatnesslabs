import Link from 'next/link'; import { CheckList, IconBadge, PageHero } from '@/components/Site';
export default async function Bio({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const name = slug.split('-').map(x => x[0].toUpperCase() + x.slice(1)).join(' '); return <div className="page-shell"><PageHero eyebrow="EXECUTIVE PROFILE" title={name} accent="."/><article className="page-section mx-auto max-w-3xl"><div className="content-card"><IconBadge name="target"/><h2 className="mt-4 text-2xl text-navy">Leadership focused on <span className="heading-accent">certainty.</span></h2><p className="mt-4 text-sm leading-7 text-slate-600">A senior TASOL leader with deep experience in marine and technical services. Their work centres on operational readiness, trusted partnerships and disciplined delivery.</p><h3 className="mt-7 text-xl text-navy">Focus areas</h3><CheckList items={['Operational excellence', 'HSE governance', 'Client value and assurance']}/><Link href="/leadership" className="focus-ring mt-7 inline-flex min-h-10 items-center text-sm font-bold text-cyan">← Meet the directorate</Link></div></article></div>; }
