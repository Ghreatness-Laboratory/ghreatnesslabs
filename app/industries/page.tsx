import { IconBadge, PageHero, SectionHeading } from '@/components/Site';
const domains = [['Oil & Gas','droplet'],['Maritime Transport','anchor'],['Power & Utilities','bolt'],['Solar Energy','sun'],['Engineering','settings'],['Ports & Terminals','building']] as const;
export default function Industries() { return <div className="page-shell"><PageHero eyebrow="INDUSTRIES" title="Fluent in the environments that move" accent="industry." copy="Sector-aware people and controls that understand the demands of the field."/><section className="page-section mx-auto max-w-7xl"><SectionHeading eyebrow="Operational Domains" title="Sector-specific capability, one accountable" accent="partner."/><div className="mt-8 grid grid-cols-2 gap-3 md:grid-cols-3">{domains.map(([name, icon]) => <article id={name.toLowerCase().replaceAll(' ','-')} className="content-card interactive-card p-4 sm:p-5" key={name}><IconBadge name={icon}/><h2 className="mt-3 text-[17px] text-navy">{name}</h2><p className="mt-2 text-xs leading-5 text-slate-600">Reliable technical services and disciplined controls for {name.toLowerCase()} requirements.</p></article>)}</div></section></div>; }
