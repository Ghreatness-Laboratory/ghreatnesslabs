import {NextResponse} from 'next/server';export async function POST(req:Request){const {email,password}=await req.json();if(email==='admin@tasolmarine.com'&&password==='TasolAdmin2026!'){const r=NextResponse.json({ok:true});r.cookies.set('tasol-admin','authenticated',{httpOnly:true,sameSite:'strict',path:'/'});return r}return NextResponse.json({error:'Invalid credentials'},{status:401})}
